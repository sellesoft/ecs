/*
 *  Attached to entities that should collide with other entities.
 */

$ require "common"
$ local metadata = require "reflect.Metadata"

@lpp.import "game/shared/component/Component.lh"

@lpp.import "math/Rect.lh"

struct Collider : Component
{
  @metadata.data_field
  Rect bounds;

  @metadata.data_field
  b8 static_pos;
};
